#include<stdio.h>
int main (){

    int x;
    printf("Enter the value of the digit : ");
    scanf("%d",&x);

    int square = x*x;
    printf("The square of the given number will be : %d",square);
    

    return 0;
}