#include<stdio.h>
int main (){

  //Write a program that takes an integer input from the user
  // and checks whether the number is positive, negative or zero using if-else statements.

    printf("Enter the input : ");
    int x;
    scanf("%d",&x);
    if (x>0)
    {
        printf("The Value Is Positive : " );


    }
    else
    {
        printf("The value is equals to 0 : ");
    }





    return 0;
}