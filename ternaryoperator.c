#include<stdio.h>
int main (){

// TERNARY OPERATOR //
int x;
printf("Enter the value of the digit : ");
scanf("%d",&x);

x%2==0 ? printf("Even number ") : printf("odd number : ");// This operator is used for writing a code in 1 line instead of 5 to 6 lines 
//you can write 2 if else conditions but if you want to write the code of nested if else then it will become complex //

    return 0;
}